name: "PR Testing"

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Use Node v22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: yarn
      - run: yarn test
