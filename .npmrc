# Some deps (e.g. nonaction) still declare React 16/17 peer ranges but work
# fine on this project's React 19. npm's strict peer resolution would abort the
# install over those stale ranges; yarn classic ignored them. Keep installs
# permissive so `npm install` / `npm ci` succeed.
legacy-peer-deps=true
